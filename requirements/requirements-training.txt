# ==============================================================================
# LemGendary Training Suite — Kaggle / Colab Runtime Requirements
# ==============================================================================
# Strip-down of requirements.txt: identical runtime deps, NO dev tooling
# (pylint / pyright / type stubs), NO GUI OpenCV, NO MT5.
#
# Install with the CUDA wheel index. The notebook generator writes a
# --constraint file to force +cu118 on P100 nodes; that constraint wins over
# the range pins below.
# ==============================================================================

--extra-index-url https://download.pytorch.org/whl/cu121

# ── Core Deep Learning ──────────────────────────────────────────────────────
torch>=2.4.0,<2.6.0
torchvision>=0.19.0,<0.21.0
torchaudio>=2.4.0,<2.6.0

# ── Scientific Stack ────────────────────────────────────────────────────────
# numpy<2.0 pin is intentional: torchmetrics[image] and pyarrow on Kaggle's
# Python 3.10 do not play nicely with numpy 2.x.
numpy>=1.26.0,<2.0.0
scipy>=1.11.0,<1.15.0
pandas>=2.1.0,<2.3.0
pyarrow>=14.0.0,<18.0.0

# ── Metrics ─────────────────────────────────────────────────────────────────
# [image] extra is MANDATORY for LPIPS + FID.
torchmetrics[image]>=1.4.0,<1.7.0
pycocotools>=2.0.7,<2.1.0
torch-fidelity>=0.3.0,<0.5.0

# ── Computer Vision ─────────────────────────────────────────────────────────
# headless is the ONLY safe choice on Kaggle/Colab (no libGL.so).
opencv-python-headless>=4.9.0.80,<4.11.0
scikit-image>=0.22.0,<0.25.0
albumentations>=1.4.0,<2.1.0
pillow>=10.2.0,<11.2.0

# ── Model Registry / Backbones ──────────────────────────────────────────────
einops>=0.7.0,<0.9.0
timm>=0.9.16,<1.1.0
safetensors>=0.4.0,<0.6.0

# ── Diffusion / Text-to-Image ───────────────────────────────────────────────
diffusers>=0.27.0,<0.32.0
transformers>=4.40.0,<4.50.0
accelerate>=0.29.0,<1.4.0
peft>=0.10.0,<0.15.0
bitsandbytes>=0.43.0,<0.46.0
sentencepiece>=0.2.0,<0.3.0

# ── Object Detection ────────────────────────────────────────────────────────
ultralytics>=8.2.0,<8.4.0

# ── Cloud Hub / Model Registry ──────────────────────────────────────────────
kaggle>=1.6.0,<2.0.0
kagglehub>=0.2.0,<0.4.0
huggingface-hub>=0.23.0,<0.28.0
gdown>=5.1.0,<5.3.0
websockets>=12.0,<15.0

# ── ONNX / Export ───────────────────────────────────────────────────────────
onnx>=1.16.0,<1.18.0
onnxsim>=0.4.36,<0.5.0
onnxconverter-common>=1.14.0,<1.17.0
onnxscript>=0.1.0,<0.3.0
# Kaggle Linux: CUDA-backed ORT.
onnxruntime-gpu>=1.18.0,<1.21.0; sys_platform == "linux"

# ── Utility ─────────────────────────────────────────────────────────────────
pyyaml>=6.0.1,<6.1.0
psutil>=5.9.0,<7.1.0
tqdm>=4.66.0,<4.68.0
requests>=2.31.0,<2.33.0
matplotlib>=3.8.0,<3.11.0

# ── Forex indicators ────────────────────────────────────────────────────────
pandas-ta-classic>=0.3.0,<0.9.0